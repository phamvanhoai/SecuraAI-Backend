import { Router } from 'express';

export const incidentManagementRouter = Router();

// Register incident-management endpoints here. Apply authentication, authorization and DTO validation per route.
