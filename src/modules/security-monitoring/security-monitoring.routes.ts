import { Router } from 'express';

export const securityMonitoringRouter = Router();

// Register security-monitoring endpoints here. Apply authentication, authorization and DTO validation per route.
